# Changelog

本项目迭代遵循「**一版一验证**」：每版出 bin + PNG 预览，真机反馈后再动下一版。
日期均为 2026 年。

## RouletteS3（轮盘盘）

### v74 — 09-07（当前版）
- 睡眠时长移到竖条正下方 (173,228)，`align=2` 走 JSON 字段级（改 raw b3 会被打包器按 JSON 重算覆盖）。
- 四角数据数字沿各自极角外移：temp/°/steps +8px、battery/calories +5px（电池 align=2 中心锚点，防 3 位数裁边）。
- 天气图标 36→42px，r=216→214 @(421,170)。
- 859,771 B。待真机验收：四角观感 / 时长位置 / 天气图标 / 4 热区跳转。

### v73 — 09-07
- 真机照片确认三修全部生效：对角线消失 ✓、时长摆正 ✓、进度条比例 ✓。

### v67–v72
- 日期行切换到公历源 `1012`/`1812`（v66 破译确认 `3012`/`3812` 是农历）。
- 各版微调素材与布局（build_v69~v72 流水线脚本）。

### v66
- **轮盘数字排列定论**：正确排列 = A(v)=90−6v（00 在 3 点、逆时针递增），红框恰好显示当前值；
  `flip_rings`/`build_v66 transform_ring()` 实现逐辐条刚性旋转。
- 温度 ° 跟随 2 位数字左移（x 296→284），消除 ~1 字宽空隙。

### v65
- 卡路里源定论（真机确认）：弧 = `1023`、数字 = `082305`；修正官方盘继承的 22° 倾斜与 spacing −4。
- 补 ep7 隐形伴侣（idx6/7，ds=1012/1812）对齐谷歌结构（AOD 修复尝试）。

### v64 — 09-07（平滑扫秒定论版）
- 修复跳秒根因：prop7Raw 通道字节（dignum b6-7=刷新 ms 非零；带表 imagelist b12-13=`00 02`）。
- 修复纹理超预算：44 张弧帧回到裁剪小图块（~141×143）。
- 修复 AOD：face record u32@+0=`0x80000000`（v60~v62 AOD 不显示的真正根因）。
- 打包器修 4 bug：showCount 覆写 dataSrc b3、styleCount 硬编码 6、AOD 0x80000000、spacing 覆写 imagelist b13。
- 建立反提取验收清单（指针签名 / 通道字节逐项核对）。

### v61–v63（教训版）
- 用 unpack 出来的 wfDef 直接重打包 → prop7Raw 被剥光 → 全通道非法 → 跳秒 + 弧消失。
  **教训：绝不要用 unpack 产物覆盖工程 wfDef 的 prop7Raw。**

### v58–v60（基准盘）
- v58：扫秒 ✓ 真机验证，作为 prop7Raw 金标准来源（随仓库保存）。
- v60：卡路里误用 `283103`（真机否决），成为反面教材。

## MinimalS3 / daynight（数字盘 / 昼夜盘）

### v5_fix（当前版）
- 冒号闪烁改「透明帧法」：1811 驱动 60 帧，偶帧字形 / 奇帧全透明，与自动换背景不再冲突。
- 自动昼夜切换统一驱动源 = `0811` + 2 帧 imageIndexList（6:00–17:59 白天 / 18:00–5:59 夜晚），显存可控。
- 数字锁中性灰 112 兼顾深浅底；问候语/标签/图标做 day/night 双版。
- AOD 过渡完美。

### v5
- 初版昼夜切换（24 帧全屏底 → 显存 20MB 问题）。

### v1–v3
- 布局与字模迭代（历史版本见 GitHub Releases / git 历史）。

## 仓库本身

### 2026-09-20
- 首次公开发布：整理为开源仓库结构（README 双语、LICENSE(MIT, 代码)、docs/FORMAT.md、CHANGELOG）。
- 脚本路径全部改为仓库根相对路径，新增 requirements.txt；`.class` 编译产物不再入库。
- deliverables 瘦身：仓库只保留最新版 bin + 预览，历史版本移至 Releases。
